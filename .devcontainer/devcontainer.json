{
    "name": "Rag Chat Bot",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    // "workspaceFolder": "/app",
    "remoteUser": "root",
    "postCreateCommand": "pip install --no-cache-dir -r requirements.txt"
}
