{
    "name": "Rag Chat Bot",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    // "workspaceFolder": "/app",
    "remoteUser": "root",
    "postCreateCommand": "python -m ensurepip --upgrade && pip install --no-cache-dir -r requirements.txt"
}
